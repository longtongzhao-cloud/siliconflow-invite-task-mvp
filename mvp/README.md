# 邀新任务台 MVP

这是技术验证后的第一阶段可运行 MVP，覆盖淘宝订单到人工奖励的本地业务闭环。真实 SiliconFlow 和淘宝写接口默认没有启用；本地演示使用不会联网的适配器。

完整的管理员、客户和抢单人操作步骤见 `USER_GUIDE.md`。

## 当前阶段结论

- 自有核心通过：SKU 容量、N 人限额、30 分钟保护、15 分钟提醒、24 小时截止、超时补做、奖励锁定和人工支付幂等。
- 本地代理登录闭环通过：手机号、演示 OTP、合成会话、AES-256-GCM 密文保存、邀请码返回和 24 小时销毁边界。
- 外部接入待现场验证：真实 SiliconFlow 登录/邀请记录接口和淘宝订单/聊天权限没有在本 MVP 中发起写调用。
- 自动化验收：16 项 API、并发、安全与生命周期测试全部通过。

## 启动

在 PowerShell 中运行：

```powershell
cd <仓库目录>\mvp
powershell -ExecutionPolicy Bypass -File .\run.ps1
```

打开 <http://127.0.0.1:8765>。

Linux/macOS：

```bash
cd <仓库目录>/mvp
./run.sh
```

本地演示凭据：

- 本站手机号验证码：`135790`
- SiliconFlow mock 验证码：`246810`
- 管理员密钥：`mvp-admin-demo`

这些值仅在 `development` 环境使用，不能用于部署。

## 核心流程

1. 管理员进入“订单管理”，按淘宝订单号和 SKU 创建订单。
2. 管理员复制客户订单链接，在第一版中人工发送给淘宝客户。
3. 客户通过 mock 代理登录取得邀请码，或手动填写 8 位邀请码/完整官方链接。
4. 任务开放后，将任务链接发送到微信群等渠道。
5. 抢单人完成本站手机号登录和支付宝收款信息登记，再领取名额。
6. 本地演示可模拟完成注册和有效认证；人工模式由管理员核对后确认。
7. 成功者生成 5 元待支付奖励，管理员转账后登记唯一支付宝流水号。

## 关键规则

- `target_n` 只能由 `SF_INVITE_1/5/10 × 购买数量` 计算。
- 数据库事务始终保持 `reward_locked + active_reservations <= target_n`。
- 保护期按服务端时间计算，恰好到 30 分钟视为超时。
- 超时用户在订单 24 小时内补做，只有仍有未保护容量时才能锁定奖励。
- 支付失败或待支付仍占用已锁奖励，不能释放给第 N+1 人。
- 同一本站账号、支付宝账号和上游用户键只能获得一次奖励。

## 数据安全

- 手机号只保存 HMAC 索引和掩码，不保存明文。
- 支付宝账号、姓名和 SiliconFlow 会话采用 AES-256-GCM 应用层加密。
- SiliconFlow OTP 仅在单次请求内使用，不进入数据库。
- 会话 AAD 绑定订单、用户引用、记录 ID 和到期时间，最长 24 小时。
- 真实生产密钥必须迁移到独立 KMS；本地环境变量方案只用于 MVP。

## 适配器模式

- `mock`：仅本地演示，不联网；完整模拟代理登录和状态变化。
- `manual`：客户手动填写邀请码，管理员人工确认邀请记录。
- `live-disabled`：真实 SiliconFlow 调用失败关闭；不能靠前端或普通运行时开关启用。

未来取得稳定接口与接入条件后，应新增独立的 live 适配器部署，并保留字段漂移熔断、401/403 熔断和人工审核降级。

## 测试

```powershell
powershell -ExecutionPolicy Bypass -File .\run-tests.ps1
```

Linux/macOS：

```bash
./run-tests.sh
```

测试覆盖完整代理登录、密文检查、SKU 映射、登录/支付宝前置条件、N=1/5/10 并发、超时补做、保护名额、24 小时截止、提醒幂等和真实调用禁用。
