# 安全说明

## 当前安全状态

本项目是本地 MVP，不是生产就绪系统。生产配置已增加启动校验，会拒绝演示密钥、示例占位值、mock 上游、mock 短信和演示数据；这不能替代 KMS、管理员 MFA、真实短信、生产数据库、备份、监控以及 SiliconFlow/淘宝的正式接入。不要将当前开发配置直接暴露到公网。

公开任务接口不得返回邀请码或官方邀请链接；这些信息只允许在抢单成功后通过本人会话读取。抢单人提交的 SiliconFlow 用户 ID 仅作为加密的待核验声明，不得直接触发注册、认证或奖励状态。远程浏览器模式在受信 HTTPS 网关、一次性 viewer、回调验签和销毁机制完成前必须保持 `disabled`。

## 报告安全问题

不要在公开 Issue 中提交漏洞利用细节、真实账号、手机号、OTP、Cookie、令牌、支付流水或数据库副本。仓库所有者应在 GitHub 创建仓库后启用 Private vulnerability reporting，并通过该渠道接收报告。

报告应包含受影响版本、复现前提、最小复现步骤、影响和建议修复；所有示例数据必须为合成或脱敏数据。

## 禁止提交

- `.env` 和任何生产配置；
- AppSecret、Access Token、Refresh Token、Cookie、SiliconFlow 会话或短信凭据；
- 真实手机号、支付宝账号/姓名、身份证、人脸或证件图片；
- `mvp/data/` 中的数据库；
- 未脱敏的网络抓包、页面截图、日志和验证证据；
- 私钥、证书私钥或 KMS 导出材料。

如果凭据误入 Git 历史，删除文件不足以解决问题：应立即吊销并轮换凭据，再清理历史并通知协作者重新同步。

## 外部平台测试

SiliconFlow 或淘宝的真实测试必须使用参与者本人控制的专用测试账号，在明确授权和人工监督下进行。不得绕过验证码、风控或权限控制，也不得把用户对本站的委托表述为外部平台对私有接口的授权。
